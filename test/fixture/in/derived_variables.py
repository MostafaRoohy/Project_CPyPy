def compute_features(prices, window):

    n = len(prices)
    total = 0.0
    count = 0

    for p in prices:
        total += p
        count += 1

    mean = total
    last = prices[-1]
    delta = last

    if x > 0 and x < 10 and y != 0:
        risk = x
        return risk

    return score
